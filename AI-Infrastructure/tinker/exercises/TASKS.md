# 🧪 模拟实战：Tinker SDK 任务清单

> 目标：用“做任务”的方式读懂 SDK，而不是停留在“看懂了”。
> 每个任务都建议产出：代码改动 + 测试 +（必要时）文档更新。

---

## Milestone 0：本地闭环

### 🟢 Task 0.1：跑通测试/类型检查/格式化
* **优先级**：P0
* **验收标准**：
  - [ ] `pytest` 全绿
  - [ ] `ruff` / `pyright` 全绿

### 🟡 Task 0.2：生成 API 文档
* **优先级**：P1
* **验收标准**：
  - [ ] `scripts/generate_docs.py` 可重复生成，产物稳定无噪音 diff

---

## Milestone 1：读懂一次请求

### 🟢 Task 1.1：画出“请求生命周期”序列图
* **优先级**：P0
* **范围**：`resources/* → _base_client.request → httpx → _response.parse`
* **验收标准**：
  - [ ] Mermaid 图 + 关键节点说明（重试点、错误映射点、解析点）

### 🟡 Task 1.2：补一个“错误映射”测试
* **优先级**：P1
* **目标**：确保 401/404/429/5xx 映射到期望的异常类型
* **验收标准**：
  - [ ] mock server 返回指定 status code
  - [ ] SDK 抛出对应异常（或 CLI 输出对应提示）

---

## Milestone 2：端到端新增一个 Endpoint

### 🟢 Task 2.1：新增 types + resource method + 测试
* **优先级**：P0
* **验收标准**：
  - [ ] `src/tinker/types/` 新类型可序列化/反序列化
  - [ ] `src/tinker/resources/` 新方法可调用并返回 typed response
  - [ ] `tests/mock_api_server.py` 增加 mock 实现
  - [ ] `pytest` 增加覆盖 happy path + 1 条 error path

### 🟡 Task 2.2：更新生成文档
* **优先级**：P1
* **验收标准**：
  - [ ] `docs/api/` 生成物包含新增 API 的文档入口

---

## Milestone 3：可靠性与并发

### 🟡 Task 3.1：验证 RetryHandler 的 progress timeout
* **优先级**：P1
* **验收标准**：
  - [ ] 构造“无进度”场景并稳定复现失败
  - [ ] 错误信息包含明确诊断（timeout 秒数、卡住提示）

### 🟡 Task 3.2：给采样路径补一个限流/退避相关测试（可选）
* **优先级**：P2
* **提示**：关注 `InternalClientHolder.sample_dispatch_rate_limit()` 的行为

---

## Milestone 4：CLI 扩展

### 🟡 Task 4.1：新增 `tinker health` 命令（并保持懒加载）
* **优先级**：P2
* **验收标准**：
  - [ ] `tinker --help` 不变慢（不模块级 import 重依赖）
  - [ ] `tinker health` 输出健康状态，错误提示友好

